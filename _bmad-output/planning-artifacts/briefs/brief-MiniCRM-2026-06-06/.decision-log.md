# Decision Log — min-crm Product Brief

Canonical memory and audit trail for the brief. Every decision, change, and override is recorded here.

## 2026-06-06 — Session start

- **Intent:** Create. Synthesis brief drawing from four inputs: brainstorming-session-2026-06-06, domain-research, technical-research, and project-context.md (the Engineering Guidelines / Standardization Constitution).
- **Mode:** Fast path. User supplied a complete 8-section structure and instructed "do not re-open settled decisions; carry them forward." Decisions already pinned in project-context are treated as settled and carried, not re-debated.
- **Workspace:** `_bmad-output/planning-artifacts/briefs/brief-MiniCRM-2026-06-06/`
- **Open forks identified for the user (not derivable, materially shape the brief):**
  1. Customer lifecycle states — constitution says `active/inactive/churned` (no transitions); brainstorm + domain research say `prospect → onboarding → active → inactive → churned`. Domain research flags A/B as 🔴 High, "cannot ship undefined."
  2. Conversion depth in the pilot — full compensating saga (brainstorm centerpiece) vs guarded transition.
  3. Team size — needed to size the "~one story per member" 2-week pilot cut.

## 2026-06-06 — Forks resolved (user)

- **DEC-1 (resolves domain-research flags A/B, 🔴 High):** Customer lifecycle = **5-state** `prospect → onboarding → active → inactive → churned`. Conversion lands a Customer in `prospect`; an onboarding workflow walks it `onboarding → active`. `inactive ↔ active` reactivation; `churned` terminal-ish. **Action for architecture:** define `CUSTOMER_TRANSITIONS` in `shared/domain/status.ts` (currently absent) and update the `CustomerStatus` union in constitution §3.1. This overrides the constitution's 3-state `active/inactive/churned` — logged as a deliberate deviation per constitution preamble.
- **DEC-2:** Lead→Customer conversion is built as the **full reference saga** — persisted, resumable, compensating workflow instance with UI-inspectable state. Graduates the Workflow seam from stub to built (brainstorm Concept #4). Conversion legal only from `qualified`; `converted` lead is terminal/read-only; bidirectional lineage.
- **DEC-3 (team size):** User declined a fixed headcount — "put the stories as it should be." Pilot cut is therefore sized to the **natural minimal vertical slice** that demonstrates full layering, structured as a coherent story set with one owner each; the team maps owners to stories by actual headcount. Not pinned to an arbitrary N.
- **Carried as ticket-state clarification (domain flag C):** recommend `closed` terminal (reopen only `resolved → open`), noted as a flag for architecture; constitution currently allows `closed → open`. Logged, not forced.
- **Carried (domain flag D):** SLA targets out of scope for pilot (no timer engine); the "SLA pauses while `pending`" semantic documented as vocabulary only.
