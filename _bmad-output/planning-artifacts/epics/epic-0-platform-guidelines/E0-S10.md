---
id: E0-S10
title: Flag/config provider + Noop external ports
epic: "E0 — Platform Guidelines & Standards (the governing contract)"
feature: "0.6 — Conformance, Flags & Testing Harness"
cut: pilot
status: backlog
depends_on: [E0-S5]
inherits_uc: [TC]
prd_story: "_bmad-output/planning-artifacts/prds/prd-MiniCRM-2026-06-06/prd.md  (§6 E0-S10)"
adrs: [ADR-011, ADR-012]
constitution_refs: ["§1", "§9"]
---

# E0-S10 — Flag/config provider + Noop external ports

> **GitHub Epic → Feature → Story:** Epic **E0 — Platform Guidelines & Standards** › Feature **0.6 — Conformance, Flags & Testing Harness** › Story **E0-S10**
> **Cut:** Pilot · **Depends on:** E0-S5 (auth context = flag evaluation context)

## Story
As the platform, feature flags and config resolve through one OpenFeature-shaped provider and all external/out-of-scope systems sit behind Noop ports with flags off, so that no vendor SDK leaks into call sites and capabilities can be toggled per tenant/subsidiary.

## Implementation-Ready Acceptance Criteria
1. **AC1 — OpenFeature-shaped hooks.** `useFlag`/`useConfig` are OpenFeature-shaped; the **evaluation context is the auth context** (E0-S5). A static provider serves the pilot, swapping to Unleash in production behind the same interface (ADR-011).
   *Touches:* `src/shared/config/FlagProvider.tsx`, `src/shared/config/useFlag.ts`, `src/shared/config/useConfig.ts` · *Seam:* flag/config provider · *Schema/Map:* n/a.
2. **AC2 — Most-specific-wins, deny-wins, cycle-proof (flag H).** Resolution precedence is `subsidiary > tenant > system`; conflicts resolve deny-wins; evaluation is cycle-proof.
   *Touches:* `src/shared/config/FlagProvider.tsx` · *Seam:* flag/config provider · *Schema/Map:* precedence rules.
3. **AC3 — External-system flags hard-off behind Noop ports (ADR-012).** Odoo/Unifonic/Cloud integrations are hard-off behind Noop ports; **no vendor SDK is referenced at any call site**.
   *Touches:* `src/shared/config/ports/{ErpSyncPort,MessagingPort,CloudPort}` (+ Noop adapters) · *Seam:* Ports & Adapters · *Schema/Map:* port interfaces.
4. **AC4 — Out-of-scope engines = port interfaces only.** Billing, multi-cloud, and AI-agent engines exist as port interfaces only (no implementations).
   *Touches:* `src/shared/config/ports/*` · *Seam:* Ports & Adapters · *Schema/Map:* port interfaces.

## Inherited Universal Conformance (subset)
- **TC — Traceability.** story → spec → code → test → `Closes #`; preview green; `sprint-status.yaml`; passes `bmad-code-review`.

## Architecture — components, seams & modules touched
- **Modules (NFR-11):** `src/shared/config/FlagProvider.tsx`, `src/shared/config/useFlag.ts`, `src/shared/config/useConfig.ts`, `src/shared/config/ports/{ErpSyncPort,MessagingPort,CloudPort}` + Noop.
- **Patterns:** OpenFeature-shaped flags; most-specific-wins/deny-wins resolution; Ports & Adapters with Noop + flags-off (the pilot's external boundary).
- **ADRs:** ADR-011 (flags), ADR-012 (external/out-of-scope = ports, Noop, flags off).

## Test Requirements (NFR-12)
- **Vitest:** resolution precedence `subsidiary > tenant > system`; deny-wins on conflict; cyclic flag definitions are rejected/short-circuited (no infinite loop); evaluation context is the auth context; external flags default off; Noop ports return inert results; an import test asserts no vendor SDK is referenced at any call site.
- **RTL:** a component reads `useFlag`/`useConfig` and renders the flagged/unflagged branch correctly.
- **Playwright:** n/a.

## Dependencies & sequencing
- Depends on **E0-S5** (auth context for evaluation). Consumed by every feature that gates capability by flag; external ports stay Noop/off for the pilot.

## References (GitHub issue body — `Closes #`)
- **PRD story:** E0-S10 — prd.md §6
- **ADR(s):** ADR-011, ADR-012
- **Constitution:** project-context.md §1, §9
- **Inherited:** TC
