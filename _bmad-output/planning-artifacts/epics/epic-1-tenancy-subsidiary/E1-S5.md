---
id: E1-S5
title: Cross-subsidiary roll-up (read model)
epic: "E1 — Tenancy & Subsidiary"
feature: "1.4 — Parent Roll-up Read Model"
cut: pilot
status: backlog
depends_on: [E1-S1, E1-S2]
inherits_uc: [UC-1, UC-5, TC]
prd_story: "_bmad-output/planning-artifacts/prds/prd-MiniCRM-2026-06-06/prd.md  (§6 E1-S5)"
adrs: [ADR-002, ADR-009, ADR-015]
constitution_refs: ["§6", "§8"]
---

# E1-S5 — Cross-subsidiary roll-up (read model)

> **GitHub Epic → Feature → Story:** Epic **E1 — Tenancy & Subsidiary** › Feature **1.4 — Parent Roll-up Read Model** › Story **E1-S5**
> **Cut:** Pilot · **Depends on:** E1-S1 (scope seam), E1-S2 (subsidiaries to aggregate)

## Story
As a tenant admin, I see aggregate counts across my subsidiaries, so that I have a parent-level view of the whole tenant.

## Implementation-Ready Acceptance Criteria
1. **AC1 — tenant_admin-only.** The roll-up surface and its aggregate query are gated by `can(actor, 'rollup', 'view', resource)` (ADR-015); only a `tenant_admin` (`subsidiaryId=null`) may view tenant-wide aggregates.
   *Touches:* `src/app/router.tsx`, `src/shared/auth/{guards.tsx,permissions.ts}`, `src/features/dashboard` · *Seam:* two-gate authZ · *Schema/Map:* n/a.
2. **AC2 — Parent-level read model aggregating subsidiary data; never a cross-boundary write.** The roll-up is a **read-only** aggregation (counts per subsidiary / per entity) computed over the admin's tenant scope. It is strictly a read model — **no cross-boundary writes** (Glossary: Roll-up); it issues no mutations and emits no domain events.
   *Touches:* `src/features/dashboard/widgets`, `src/shared/data/LocalStorageRepository.ts` (read-only `list`/count) · *Seam:* `Repository<T>.list` aggregation (read-only) · *Schema/Map:* n/a.
3. **AC3 — Subsidiary user sees only their own scope (UJ-5 edge case).** A subsidiary user opening the same surface sees aggregates for **their own scope only** (own subsidiary + parent-level), never sibling-subsidiary data — the read model re-uses the E1-S1 scope filter, so it cannot disclose cross-subsidiary counts.
   *Touches:* `src/shared/data/LocalStorageRepository.ts`, `src/shared/auth/permissions.ts`, `src/features/dashboard` · *Seam:* repository scope filter · *Schema/Map:* n/a.
4. **AC4 — Four states (UC-1).** The roll-up dashboard renders loading/empty/error/data via `<QueryStateBoundary>`; a tenant with no subsidiary data shows the empty state, not an error.
   *Touches:* `src/shared/ui/templates/Dashboard`, `src/shared/ui/QueryStateBoundary.tsx`, `src/features/dashboard/widgets` · *Seam:* four-state boundary · *Schema/Map:* n/a.

## Inherited Universal Conformance (subset)
- **UC-1 — Four UI states.** Dashboard widgets render all four states via `<QueryStateBoundary>`.
- **UC-5 — Tenant scope.** Aggregation is bounded by the auth-context scope; admin gets tenant-wide, subsidiary user gets own + parent-level only — never cross-tenant or cross-subsidiary leakage.
- **TC — Traceability.** story → spec → code → test → `Closes #`; preview green; `sprint-status.yaml`; passes `bmad-code-review`.

## Architecture — components, seams & modules touched
- **Modules (NFR-11):** `src/features/dashboard/widgets`, `src/shared/ui/templates/Dashboard`, `src/shared/ui/QueryStateBoundary.tsx`, `src/shared/data/LocalStorageRepository.ts`, `src/shared/auth/{guards.tsx,permissions.ts}`, `src/app/router.tsx`.
- **Patterns:** read-only aggregation over the repository scope filter, four-state boundary, two-gate authZ. No 4-beat (read-only — no mutation, no events).
- **ADRs:** ADR-002 (tenant/subsidiary scope; `null`=parent-level), ADR-009 (claims, two gates, 404-not-403), ADR-015 (`view` grant gates the roll-up; subsidiary user scoped to own).

## Test Requirements (NFR-12)
- **Vitest:** aggregation correctness — admin counts span all tenant subsidiaries + parent-level; subsidiary user counts cover only own + parent-level; read model issues no writes/events (assert no mutation path invoked); permission cells — `view` granted to tenant_admin, scoped for subsidiary user, denied where applicable.
- **RTL:** Dashboard four states (loading/empty/error/data); empty-tenant shows empty state; widgets render counts for the resolved scope.
- **Playwright:** admin roll-up shows tenant-wide aggregates; **cross-tenant isolation** — roll-up never includes another tenant's counts; **UJ-5 edge case** — subsidiary user on the same surface sees only their own scope's aggregates, no sibling-subsidiary numbers.

## Dependencies & sequencing
- Prereqs: E1-S1 (scope seam reused for aggregation), E1-S2 (subsidiaries to aggregate); benefits from E1-S3 (post-offboard counts) and E1-S4 (scope context).
- Unblocks: parent-level visibility for UJ-1; foundational read-model pattern reused by Epic 5 dashboard widgets.

## References (GitHub issue body — `Closes #`)
- **PRD story:** E1-S5 — prd.md §6
- **ADR(s):** ADR-002, ADR-009, ADR-015
- **Constitution:** project-context.md §6, §8
- **Inherited:** UC-1, UC-5 + TC
